import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  turbopack: {
    // Especifique explicitamente a raiz do projeto
    root: "/Users/joaopiedade/Desktop/Projects/Startup/umami-connect"
  }
};

export default nextConfig;
